<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baseball.game.mapper.MemberMapper">
	<select id="login" resultType="boolean">
		select count(*) from member where id=#{Id} and pw=#{Pw}
	</select>
	
	<select id="checkId" resultType="boolean">
		select count(*) from member where id=#{Id}
	</select>
	
	<insert id="register">
		insert into member(Id,Pw,Team) values (#{Id},#{Pw},#{Team})
	</insert>
	
	<select id="member" resultType="com.baseball.game.dto.member">
		select * from member where id=#{Id}
	</select>
</mapper>